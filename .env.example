# Credenciais NUNCA vão para o repositório (ele é público).
# Copie para .env e preencha localmente / nas Environment Variables da Vercel.
MONGODB_URI="mongodb+srv://usuario:senha@cluster.mongodb.net/rocketstarDB?retryWrites=true&w=majority"
JWT_SECRET="gere-com-openssl-rand-hex-32"
